---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="flex flex-wrap justify-center items-center gap-x-2">
  {
    tags.map((tag) => (
      <li class="border border-slate-300 dark:border-slate-700 rounded-md px-1.5">
        {tag}
      </li>
    ))
  }
</ul>
