---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="flex flex-wrap justify-center items-center gap-x-2">
  {
    tags.map((tag) => (
      <li class="border border-gray-300 rounded-md px-1.5">{tag}</li>
    ))
  }
</ul>
